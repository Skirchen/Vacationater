<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.D308Vacationater"
        tools:targetApi="31">
        <activity
            android:name="skirchen.example.Vacationater.ui.ReportsActivity"
            android:exported="false" />

        <receiver
            android:name="skirchen.example.Vacationater.ui.MyReceiver"
            android:enabled="true"
            android:exported="true" />

        <activity
            android:name="skirchen.example.Vacationater.ui.ExcursionDetail"
            android:exported="false" />
        <activity
            android:name="skirchen.example.Vacationater.ui.VacationDetail"
            android:exported="false"
            android:label="Vacation Details" />
        <activity
            android:name="skirchen.example.Vacationater.ui.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>